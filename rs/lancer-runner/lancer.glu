let { Eff } = import! std.effect
let { TestCluster } = import! lancer.effect.test_cluster
let { Compiler } = import! lancer.effect.compiler

type Lancer r = Eff [|
    test_cluster: TestCluster,
    compiler: Compiler,
    | r |] ()

{
    Lancer,
}
