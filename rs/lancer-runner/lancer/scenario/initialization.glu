let prim = import! lancer.test_cluster.builder.prim
let effect @ { Eff, ? } = import! std.effect
let { Lift, ? } = import! std.effect.lift
let p @ { Preparation } = import! lancer.scenario.preparation
let { SuiAddress } = import! lancer.sui.sui_address

type Initialization = {
    generate_keypair: () -> Eff [| lift : Lift IO | r |] SuiAddress,
    start: () -> Eff [| lift : Lift IO | r |] Preparation,
}

{
    Initialization,
}
